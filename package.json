{
  "name": "codex-persistent-mode",
  "version": "1.0.0",
  "description": "Auditable, installable recreation of a safe global Persistent mode for Codex CLI 0.145.0",
  "type": "module",
  "bin": {
    "persist": "bin/persist.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "test:verbose": "node --test --test-reporter=spec \"test/**/*.test.js\""
  },
  "license": "MIT",
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
